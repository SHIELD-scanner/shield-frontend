
import type { Meta } from "@storybook/react";
import { Card } from "./card";

export default {
  title: "shadcn-ui/Card",
  component: Card,
} as Meta<typeof Card>;

export const Default = {
  args: {},
};
