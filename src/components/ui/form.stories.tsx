import type { Meta } from "@storybook/react";
import { Form } from "./form";

export default {
  title: "shadcn-ui/Form",
  component: Form,
} as Meta<typeof Form>;

export const Default = {
  args: {},
};
