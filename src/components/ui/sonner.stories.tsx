import type { Meta } from "@storybook/react";
import { Sonner } from "./sonner";

export default {
  title: "shadcn-ui/Sonner",
  component: Sonner,
} as Meta<typeof Sonner>;

export const Default = {
  args: {},
};
