import type { Meta } from "@storybook/react";
import { Tabs } from "./tabs";

export default {
  title: "shadcn-ui/Tabs",
  component: Tabs,
} as Meta<typeof Tabs>;

export const Default = {
  args: {},
};
